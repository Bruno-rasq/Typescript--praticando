enum Directions {
  Up = 1,
  Right,
  Down,
  Left
}

// o valor Up foi inicializado como 1, todos os outros serão incrementados apartir deste.

// Up = 1
// Right = 2
// Down = 3
// Left = 4

// caso não fosse inicializado os valores seriam incrementados do 0